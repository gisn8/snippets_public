The goal of this script is to use when the laptop switches power sources such as when plugging or unplugging from AC to battery power.
The script itself works, but still looking into how to trigger it.
